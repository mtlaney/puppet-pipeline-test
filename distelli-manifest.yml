mtlaney/puppet-pipeline-test:
  Build:
    - npm install
  PkgInclude:
    - '*'
  PreInstall:
    - sudo apt-get -y update
    - sudo apt-get -y install nodejs
  Env:
    - PORT: '3000'
  Exec:
    - npm start
    # comment
